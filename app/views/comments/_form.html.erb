<%= simple_form_for([@product, @product.comments.build]) do |f|%>

  <div class="ufeed-content">
      <%= f.text_area :content, class: "form-control" %>
  </div>
  <br>
  <%= f.submit "发表评论", class: "btn btn-orange" %>
<% end %>
